> This file is autogenerated. Do not edit it manually.

# FlareCog Multi-Tenant Architecture with Workers for Platforms

**Date:** November 23, 2025

**Author:** Manus AI

## 1. Introduction

This document provides a detailed overview of the FlareCog multi-tenant cognitive architecture, enabled by the integration of CloudFlare Workers for Platforms. This new architecture transforms FlareCog from a single-instance cognitive system into a scalable, secure, and customizable Software-as-a-Service (SaaS) platform. By leveraging dispatch namespaces, FlareCog can now offer isolated cognitive environments to thousands or even millions of users, each with their own dedicated AtomSpace and MindAgent instances.

## 2. Architectural Vision

The primary goal of this architectural evolution is to democratize access to advanced cognitive computing. By creating a multi-tenant platform, FlareCog can serve a wide range of users, from individual researchers to large enterprises, each with their own unique cognitive workloads. The architecture is designed to be **scalable, resilient, and secure**, providing a robust foundation for building and deploying distributed AI applications.

## 3. Core Components

The multi-tenant architecture is composed of three main components, as illustrated in the diagram below.

```mermaid
graph TD
    A[Internet / Users] --> B{FlareCog Dispatch Worker};
    B --> C{Dispatch Namespace: "flarecog"};
    C --> D[User Worker 1<br>(Tenant: acme)];
    C --> E[User Worker 2<br>(Tenant: corp)];
    C --> F[...];

    subgraph "Platform Layer"
        B;
    end

    subgraph "Tenant Layer"
        C;
        D;
        E;
        F;
    end

    style B fill:#f9f,stroke:#333,stroke-width:2px
    style C fill:#ccf,stroke:#333,stroke-width:2px
```

### 3.1. Dynamic Dispatch Worker

The **Dynamic Dispatch Worker** is the single entry point for all incoming traffic to the FlareCog platform. It acts as a sophisticated reverse proxy that programmatically routes requests to the appropriate tenant-specific cognitive instance. Its key responsibilities are summarized in the table below.

| Responsibility | Description |
| :--- | :--- |
| **Tenant Resolution** | Identifies the tenant associated with each request using strategies like subdomain, header, or path analysis. |
| **Authentication** | Verifies the identity of the user and ensures they are authorized to access the requested tenant. Supports API keys and JWTs. |
| **Authorization** | Enforces permissions, ensuring that users can only perform operations they are authorized for (e.g., read, write, admin). |
| **Rate Limiting** | Applies tenant-specific rate limits to prevent abuse and ensure fair resource allocation across the platform. |
| **Analytics Tracking** | Gathers usage data for billing, monitoring, and platform-wide cognitive analytics. |
| **Request Routing** | Forwards the validated and enriched request to the correct user Worker within the dispatch namespace. |

### 3.2. Dispatch Namespace

The **Dispatch Namespace** is a CloudFlare-managed container that holds all the user Workers. For the FlareCog platform, a single dispatch namespace named `flarecog` is used. This namespace allows the dispatch Worker to dynamically look up and invoke any tenant's user Worker by its unique name (which corresponds to the tenant ID).

### 3.3. User Workers

Each tenant on the FlareCog platform is provisioned with their own **User Worker**. This is a dedicated, isolated instance of the FlareCog cognitive engine. Each user Worker has its own set of resources, ensuring complete separation of data and processing between tenants.

Key features of a user Worker include:

-   **Isolated Durable Objects**: Each user Worker gets its own `AtomSpace` and `MindAgent` Durable Objects, named with the tenant ID to guarantee uniqueness and isolation.
-   **Tenant-Specific Configuration**: Cognitive parameters, such as reasoning strategies, agent configurations, and relevance settings, can be customized on a per-tenant basis.
-   **Shared Core Logic**: While data and configuration are isolated, all user Workers share the same underlying core logic for reasoning, learning, and perception, ensuring that all tenants benefit from platform-wide updates and improvements.

## 4. Request Lifecycle

A typical request to the FlareCog platform follows the lifecycle outlined below:

1.  A user sends a request to their tenant-specific endpoint (e.g., `https://acme.flarecog.ai/atomspace/atoms`).
2.  The request is received by the **Dynamic Dispatch Worker**.
3.  The dispatch Worker resolves the tenant ID ("acme") from the subdomain.
4.  It authenticates the request using the provided API key or JWT.
5.  It checks if the request is within the tenant's rate limits.
6.  The dispatch Worker looks up the user Worker named "acme" in the `flarecog` dispatch namespace.
7.  It enriches the request with headers like `X-Tenant-ID` and `X-User-ID`.
8.  The request is forwarded to the "acme" user Worker.
9.  The user Worker receives the request, identifies the tenant from the header, and instantiates the tenant-specific `AtomSpace` and `MindAgent` Durable Objects.
10. The cognitive operation is performed within the tenant's isolated environment.
11. The response is returned to the dispatch Worker.
12. The dispatch Worker adds rate limit headers to the response and logs analytics data.
13. The final response is sent back to the user.

## 5. Security and Scalability

This architecture was designed with security and scalability as top priorities.

-   **Security**: Tenant isolation is enforced at multiple layers. Durable Objects are named with tenant IDs, and all requests are authenticated and authorized at the dispatch layer. This prevents any possibility of cross-tenant data access.
-   **Scalability**: Workers for Platforms is designed to scale to millions of user Workers. The dispatch Worker can handle a massive volume of routing decisions, and the serverless nature of CloudFlare Workers means that resources are allocated dynamically as needed.

## 6. Conclusion

The integration of Workers for Platforms provides a powerful and elegant solution for building a multi-tenant cognitive architecture. It allows FlareCog to evolve from a single-user system into a true platform for distributed AI, offering isolated, customizable, and scalable cognitive instances to a global user base. This architecture not only enhances the capabilities of FlareCog but also provides a solid foundation for future commercialization and growth.
